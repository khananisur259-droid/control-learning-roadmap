# 版本记录

## `ROADMAP TREE V1.0.5` - 2026-08-24

- 修复 GitHub Pages 更新后浏览器可能继续使用旧缓存、出现页面无样式的问题。
- 为 CSS、知识树数据、代码模板和主脚本增加统一版本查询参数，避免新旧资源混用。
- 已确认公网 CSS 正常返回 `200` 和 `text/css`，并用全新浏览器验证三栏布局与节点样式。
- 本版本不修改学习内容、固件、控制参数、模型、机械文件或烧录文件。

## `ROADMAP TREE V1.0.4` - 2026-08-24

- 将“八路加权循迹”统一修正为“多路传感器加权循迹”，明确八路只是现有项目实例。
- 加权偏差模板取消固定长度 `[8]`，改由 `sensor_count` 和传感器位置数组决定路数。
- 模板可用于三路、五路、六路、八路等不同阵列，并明确数组顺序、极性和位置权重必须按硬件适配。
- 同步更新学习主线、P4 实训和权重示例；历史项目仍保留其实际使用八路传感器的事实。
- 本版本不修改任何固件、控制参数、模型、机械文件或烧录文件。

## `ROADMAP TREE V1.0.3` - 2026-08-24

- 学习树改为只面向控制方向学习者，移除负责人、分工、周会和考核分支。
- 取消固定 24 周安排，改为按前置知识与掌握条件推进。
- 节点由 183 个调整为 176 个，保留控制、实训、算法、具身智能和排障五条主分支。
- 新增 22 套可靠的中文注释代码模板，映射到 33 个关键学习节点。
- 模板覆盖 PID、串级控制、循迹、编码器、滤波、状态机、路径规划、遥测带宽等常用能力。
- 每套模板说明可直接复用部分、必须适配部分、实现原理和权威参考来源。
- 新增一键复制、复制失败回退、搜索模板内容和标题精确命中优先排序。
- 完成桌面端与 390x844 手机端验证；长代码可横向滚动，无页面溢出或资源错误。
- 本版本不修改任何固件、控制参数、模型、机械文件或烧录文件。

## `ROADMAP TREE V1.0.2` - 2026-08-24

- 发布到公开 GitHub 仓库 `khananisur259-droid/control-learning-roadmap`。
- 使用 GitHub Pages 提供长期公网访问入口。
- 新增仓库 README，说明在线访问、离线使用和功能范围。
- 公开内容仅包含交互式学习树及说明文档，不包含整车固件、模型、参数和其他项目资料。

## `ROADMAP TREE V1.0.1` - 2026-08-24

- 完成 183 个节点与 183 个唯一 ID 的完整性检查。
- 完成桌面端与 390x844 手机端截图验收，目录和详情抽屉均可正常操作。
- 验证搜索“卡尔曼”可定位 KF、EKF、UKF，筛选后右侧详情同步到对应范围。
- 验证进度标记、浏览器本地保存及清理逻辑，发布前已清空测试进度。
- 增加本地 SVG 网站图标声明，页面离线运行不依赖 CDN。
- 进度条动画改用 `transform`，避免宽度动画触发布局重算。
- 本版本不修改任何固件、参数、模型或烧录文件。

## `ROADMAP TREE V1.0.0` - 2026-08-24

- 创建控制方向交互式学习树。
- 数据来源为 `07_新人培养` 的七份路线、算法、实训、具身智能、踩坑和带队文档。
- 支持分支展开/折叠、全展开/收起、搜索、筛选、缩放、拖动、定位和节点详情。
- 支持浏览器本地学习进度、JSON 导入、导出和清空。
- 版本号在页面顶部可见。
- 本版本不修改任何固件、参数、模型或烧录文件。
- 浏览器视觉与交互验证已在后续 `V1.0.1` 完成。
