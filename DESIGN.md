# Design

## Direction Contract

**THESIS**：把控制学习路线做成一张真实可操作的系统框图，拒绝课程卡片墙和静态脑图。  
**OWN-WORLD**：石墨色控制台顶栏、浅灰实验台外壳、带微弱工程点阵的信号画布、白色控制节点、森林绿主强调和少量琥珀搜索提示；节点是控制模块，连线是数据流。
**STORY**：用户先看完整知识拓扑，再筛选或搜索节点，在右侧确认原理、实训与验收；核心节点可展开代码模板并一键复制，最后记录完成状态。
**FIRST VIEWPORT**：顶部紧凑工具栏，左侧路线目录，中间全尺寸树画布，右侧节点详情；搜索与范围筛选始终可见。  
**FORM**：Operate 模式的三栏工程工具；桌面完整三栏，窄屏折叠为画布加抽屉。精确需求已固定，不进行营销式概念竞赛。

## Visual System

### Palette

- 页面外壳：`#e6ebe8`
- 控制台顶栏：`#17251f`
- 主画布：`#f3f6f4`
- 一级面板：`#ffffff`
- 深色文字：`#18231e`
- 次级文字：`#5c6b64`
- 分隔线：`#cfd8d3`
- 强调色：`#0f7a58`
- 强调浅色：`#e2f2ec`
- 搜索提示：`#b86b1d`
- 警告与错误只使用语义色，不作为装饰。

禁止装饰渐变、发光、玻璃拟态、装饰圆球和无意义大阴影。画布允许使用低对比度规则点阵，因为它承担系统图坐标参照作用。

### Typography

- 界面字体：`Segoe UI`, `Microsoft YaHei UI`, `Noto Sans SC`, sans-serif。
- 代码、版本和单位：`Cascadia Mono`, `Consolas`, monospace。
- 固定字号，不随视口宽度缩放。
- 顶部标题 18px；面板标题 14px；正文 13px；辅助文字 12px。

### Geometry

- 顶栏高度 64px。
- 桌面左栏 284px，右栏 460px，中间画布自适应。
- 节点固定宽度 224px，高度 82px，圆角 7px。
- 按钮和输入圆角 5 至 7px；卡片不嵌套。
- 边框承担结构，阴影只用于浮层。

## Interaction

- 单击节点：选中并打开详情。
- 节点上的展开按钮：只控制该分支。
- 鼠标滚轮：以指针为中心缩放。
- 拖动空白画布：平移。
- 搜索：即时结果；选择结果时自动展开祖先并居中。
- 筛选：只改变可见范围，不删除进度。
- 完成状态：本地保存；父节点显示后代完成比例。
- 代码模板：在详情中使用独立代码区，顶部固定显示语言、模板用途和复制按钮。
- 复制按钮：复制完整代码，成功后短暂显示“已复制”，不改变详情布局。
- 动画 160 至 220ms，只表达展开、选中和面板切换。

## Code Template Rules

- 代码使用等宽字体、深色文字和浅灰代码底，不使用编辑器彩虹配色。
- 中文注释只解释控制意图、单位、假设和必须适配的位置，不逐行复述语法。
- 每个模板在代码前显示“可直接复用”和“必须修改”两项边界。
- 长代码块横向滚动，不挤压右侧详情宽度。
- 来源链接使用普通文本链接，不伪装成认证标识。

## Responsive Rules

- `>= 1200px`：三栏完整显示。
- `768px - 1199px`：左栏默认折叠，右栏保留。
- `< 768px`：画布全宽，目录和详情用互斥侧滑抽屉；顶部工具分两行。
- 所有固定格式控件必须保持稳定尺寸，动态内容不能改变工具栏高度。

## States

- 加载：画布显示结构骨架。
- 空搜索：显示“没有匹配节点”和清除操作。
- 筛选为空：显示当前范围没有节点。
- 数据错误：显示错误原因和重新载入操作。
- 本地存储失败：非阻塞提示，页面仍可浏览。
- 键盘焦点：2px 强调色轮廓。
