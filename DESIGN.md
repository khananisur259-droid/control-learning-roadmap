# Design

## Direction Contract

**THESIS**：把控制学习路线做成一张真实可操作的系统框图，拒绝课程卡片墙和静态脑图。  
**OWN-WORLD**：浅灰实验台外壳、白色信号画布、石墨文字、森林绿单一强调色；节点是控制模块，连线是数据流。  
**STORY**：用户先看完整知识拓扑，再筛选或搜索节点，在右侧确认目标、实训、验收与踩坑，最后记录完成状态。  
**FIRST VIEWPORT**：顶部紧凑工具栏，左侧路线目录，中间全尺寸树画布，右侧节点详情；搜索与范围筛选始终可见。  
**FORM**：Operate 模式的三栏工程工具；桌面完整三栏，窄屏折叠为画布加抽屉。精确需求已固定，不进行营销式概念竞赛。

## Visual System

### Palette

- 页面外壳：`#e9edeb`
- 主画布：`#f8faf9`
- 一级面板：`#ffffff`
- 深色文字：`#17211d`
- 次级文字：`#5c6b64`
- 分隔线：`#cfd8d3`
- 强调色：`#13795b`
- 强调浅色：`#e2f2ec`
- 警告与错误只使用语义色，不作为装饰。

禁止渐变、发光、玻璃拟态、装饰圆球和无意义大阴影。

### Typography

- 界面字体：`Segoe UI`, `Microsoft YaHei UI`, `Noto Sans SC`, sans-serif。
- 代码、版本和单位：`Cascadia Mono`, `Consolas`, monospace。
- 固定字号，不随视口宽度缩放。
- 顶部标题 18px；面板标题 14px；正文 13px；辅助文字 12px。

### Geometry

- 顶栏高度 58px。
- 桌面左栏 272px，右栏 360px，中间画布自适应。
- 节点固定宽度 210px，最小高度 68px，圆角 6px。
- 按钮和输入圆角 5px；卡片不嵌套。
- 边框承担结构，阴影只用于浮层。

## Interaction

- 单击节点：选中并打开详情。
- 节点上的展开按钮：只控制该分支。
- 鼠标滚轮：以指针为中心缩放。
- 拖动空白画布：平移。
- 搜索：即时结果；选择结果时自动展开祖先并居中。
- 筛选：只改变可见范围，不删除进度。
- 完成状态：本地保存；父节点显示后代完成比例。
- 动画 160 至 220ms，只表达展开、选中和面板切换。

## Responsive Rules

- `>= 1200px`：三栏完整显示。
- `768px - 1199px`：左栏默认折叠，右栏保留。
- `< 768px`：画布全宽，目录和详情用互斥侧滑抽屉；顶部工具分两行。
- 所有固定格式控件必须保持稳定尺寸，动态内容不能改变工具栏高度。

## States

- 加载：画布显示结构骨架。
- 空搜索：显示“没有匹配节点”和清除操作。
- 筛选为空：显示当前范围没有节点。
- 数据错误：显示错误原因和重新载入操作。
- 本地存储失败：非阻塞提示，页面仍可浏览。
- 键盘焦点：2px 强调色轮廓。

