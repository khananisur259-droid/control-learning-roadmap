# 控制方向交互式学习树产品需求

版本：`ROADMAP TREE V1.1.0`

## 目标用户

从零学习嵌入式控制、机器人控制和具身智能基础的学生。

## 核心场景

1. 学习者查看完整学习顺序和前置关系，不受固定周数约束。
2. 搜索某个算法，确认用途、条件和相关项目。
3. 在核心节点直接阅读、复制带中文注释的通用代码模板。
4. 明确模板中可以直接复用的算法部分，以及必须适配的硬件接口、单位和周期。
5. 记录个人学习进度并导入导出。
6. 遇到问题时从排障分支按硬件、通信、测量、控制顺序定位。

## 页面结构

单页面工具：

- 顶部工具栏：产品名、版本、搜索、范围筛选、树操作、进度操作。
- 左侧目录：完整层级、展开状态、完成状态、节点数量。
- 中央画布：横向树、连接线、缩放、拖动、定位和空状态。
- 右侧详情：类型、学习顺序、说明、知识点、任务、验收、踩坑、代码模板、依据来源和完成状态。
- 移动端：目录与详情改为抽屉，画布保持主界面。

## 视觉关键词

工程、克制、清晰、密集但不拥挤、系统框图、可追溯。

## 交互要求

- 展开/闭合单分支；全部展开/全部闭合。
- 画布缩放、拖动、复位和选中居中。
- 中文全文搜索和范围筛选。
- 搜索命中高亮，结果可逐项定位。
- 完成状态本地持久化；JSON 导入、导出、清空。
- 原始 Markdown 链接可直接打开。
- 代码模板一键复制；复制成功和失败都给出明确反馈。
- 模板包含中文注释、输入输出、单位、调用示例和移植检查项。
- 代码模板按节点独立维护，不与知识树结构硬编码在一起。
- 键盘可操作，减少动画设置有效。

## 状态要求

- 加载状态：稳定尺寸骨架。
- 空状态：无搜索词时不显示无意义提示。
- 无结果：显示条件和清除按钮。
- 错误状态：数据初始化错误可重试。
- 保存失败：提示浏览器未允许本地存储，不阻断浏览。
- 导入错误：说明文件格式不正确，不覆盖原进度。
- 复制失败：自动回退到兼容复制方式，仍失败时提示手动选择代码。

## 内容边界

- 不包含负责人带队、分工、考核和资产管理内容。
- 不规定 24 周或其他固定学习时间，只表达先修关系和推荐顺序。
- 模板优先使用平台无关 C/C++ 或 Python，硬件寄存器和 HAL 调用留出明确适配接口。
- 循迹算法按“多路传感器”讲解，不把现有项目使用的八路硬件误写成算法限制。
- 模板是算法最小实现，不声称无需检查即可直接烧录到任意硬件。
- 公式和接口优先引用 ARM CMSIS-DSP、TI、ROS、Python 等官方文档或公开教材。

## 移动端适配

- 顶栏允许搜索单独占一行。
- 目录和详情不同时覆盖画布。
- 触控节点和按钮最小 40px。
- 画布支持双指缩放由浏览器手势和按钮共同兜底。
